Monte Carlo estimation of Pi.  Generate random (X,Y) pairs inside unit square
and count fraction that fall within unit circle.

- pi/pi_simple -- simple version

- pi/pi_curand -- use CURAND library to create the random numbers on the GPU
